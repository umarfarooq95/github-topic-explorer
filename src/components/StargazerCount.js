import React from 'react';

export default function StartGazerCount ({count}) {

        return (
               <span className=" stargazer-count badge bg-primary rounded-pill float-right ">{count}</span> 
        )
    
}